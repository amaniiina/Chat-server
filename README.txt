Chat Server with select

Included Files:
chatserver.c: an implementation of a a simple TCP chat server that reads text from clients and sends to all connected clients including the sender himself. The server runs on the localhost address.


Notes:
- Command line usage: ./chatserver <port> <max_clients>

